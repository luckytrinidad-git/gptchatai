version: "3.9"

services:

  api:
    build: ./gptchatbot-api
    container_name: gptchatbot-api
    ports:
      - "8000:8000"
    volumes:
      - ./gptchatbot-api:/app
    restart: always

  ui:
    build: ./gptchatbot-ui
    container_name: gptchatbot-ui
    ports:
      - "8501:8501"
    volumes:
      - ./gptchatbot-ui:/app
    depends_on:
      - api
    restart: always
